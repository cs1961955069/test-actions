name: asset

on:
  push:
    tags:
      - '*'

jobs:
  build:
    name: Publish binaries
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: test
      run: ls
    - name: Build
      run: echo "build"
    - name: Upload binaries to release
      uses: svenstaro/upload-release-action@v1-release
      with:
        repo_token: ${{ secrets.TOKENA }}
        file: a.tar.gz
        asset_name: a.tar.gz
        tag: ${{ github.ref }}
        overwrite: true
